# JSON数据导入指南

> **更新日期**: 2026-01-16  
> **功能**: 支持导入完整的知识图谱JSON数据

---

## 一、快速开始 🚀

### 1. 启动系统

```bash
# 终端1: 启动后端
cd backend
npm start

# 终端2: 启动前端
cd frontend
npm start
```

### 2. 导入数据

1. 打开浏览器访问 `http://localhost:3000`
2. 点击顶部 **"导入数据"** 按钮
3. **默认显示"JSON文件"选项卡**
4. 点击 **"选择JSON文件"** 按钮
5. 选择你的JSON数据文件（如：`data/core-domain-data.json`）
6. 确认文件信息后，点击 **"导入"** 按钮

---

## 二、JSON格式要求 📋

### 2.1 基本结构

```json
{
  "version": "1.0.0",
  "name": "知识图谱名称",
  "description": "描述信息（可选）",
  "nodes": [],
  "edges": []
}
```

**必需字段**:
- `nodes`: 节点数组
- `edges`: 边数组

**可选字段**:
- `version`: 数据版本
- `name`: 数据集名称
- `description`: 描述信息
- `statistics`: 统计信息

### 2.2 节点格式

```json
{
  "id": "唯一标识符",
  "type": "实体类型",
  "data": {
    "属性名1": "属性值1",
    "属性名2": "属性值2"
  }
}
```

**示例**:
```json
{
  "id": "fr-001-lane-keeping",
  "type": "FeatureRequirement",
  "data": {
    "id": "fr-001-lane-keeping",
    "title": "高速车道保持功能",
    "epicId": "epic-highway-driving",
    "domain": "智能驾驶",
    "priority": "P0",
    "status": "implementing",
    "owner": "FO-李四"
  }
}
```

### 2.3 边格式

```json
{
  "id": "边的唯一标识符",
  "source": "源节点ID",
  "target": "目标节点ID",
  "type": "关系类型",
  "data": {
    "属性名": "属性值"
  }
}
```

**示例**:
```json
{
  "id": "e-epic-fr-lka",
  "source": "epic-highway-driving",
  "target": "fr-001-lane-keeping",
  "type": "splits_to_fr",
  "data": {
    "relationship": "Epic拆分为车道保持FR"
  }
}
```

---

## 三、完整示例 📝

### 3.1 简单示例（2个节点，1条边）

```json
{
  "version": "1.0.0",
  "name": "简单示例",
  "nodes": [
    {
      "id": "epic-001",
      "type": "Epic",
      "data": {
        "title": "高速智能驾驶",
        "priority": "P0",
        "status": "implementing"
      }
    },
    {
      "id": "fr-001",
      "type": "FeatureRequirement",
      "data": {
        "title": "车道保持功能",
        "epicId": "epic-001",
        "priority": "P0",
        "status": "implementing"
      }
    }
  ],
  "edges": [
    {
      "id": "e-001",
      "source": "epic-001",
      "target": "fr-001",
      "type": "splits_to_fr",
      "data": {
        "relationship": "Epic拆分为FR"
      }
    }
  ]
}
```

### 3.2 完整示例

参考项目提供的示例文件：
- **`data/core-domain-data.json`** - 核心领域模型数据（69节点，74边）⭐ 推荐
- `data/sample-data.json` - 智能驾驶样本数据

---

## 四、导入流程详解 🔄

### 4.1 前端处理

1. **文件选择**: 用户选择JSON文件
2. **文件读取**: 使用FileReader读取文件内容
3. **JSON解析**: 解析JSON字符串为对象
4. **格式验证**: 
   - 检查是否包含`nodes`和`edges`字段
   - 检查它们是否为数组
5. **显示预览**: 显示节点数和边数
6. **用户确认**: 点击"导入"按钮
7. **发送请求**: 调用后端API `/api/v1/import/json`
8. **显示结果**: 显示导入成功的节点数和边数

### 4.2 后端处理

1. **接收数据**: 接收`nodes`和`edges`数组
2. **数据验证**: 验证数组格式
3. **数据导入**: 调用GraphService的`importData`方法
4. **数据合并**: 
   - 新节点添加到现有节点列表
   - 重复ID的节点会被更新
   - 新边添加到现有边列表
5. **数据持久化**: 保存到内存/文件
6. **返回结果**: 返回导入的节点数和边数

---

## 五、常见问题 ❓

### Q1: JSON文件解析失败怎么办？

**可能原因**:
- JSON格式不正确（缺少引号、逗号等）
- 包含非法字符
- 文件编码问题

**解决方法**:
1. 使用JSON格式验证工具（如：jsonlint.com）验证格式
2. 确保文件编码为UTF-8
3. 检查是否有中文引号或特殊字符
4. 使用VS Code等编辑器的JSON格式化功能

### Q2: JSON格式错误："必须包含nodes和edges字段"

**原因**: JSON缺少必需的`nodes`或`edges`字段

**解决方法**:
```json
{
  "nodes": [],  // 必需，即使为空也要有
  "edges": []   // 必需，即使为空也要有
}
```

### Q3: 导入后图谱没有显示？

**可能原因**:
- 节点数量为0
- 节点ID重复导致覆盖
- 浏览器需要刷新

**解决方法**:
1. 检查JSON文件的`nodes`数组是否为空
2. 刷新页面重新加载数据
3. 检查浏览器控制台是否有错误信息

### Q4: 如何验证数据是否导入成功？

**方法1**: 查看导入提示
```
导入成功: 69个节点, 74条边
```

**方法2**: 查看图谱视图
- 图谱会自动渲染新节点和边

**方法3**: 查看表格视图
- 切换到"表格视图"查看所有节点

**方法4**: 通过API查询
```bash
curl http://localhost:3001/api/v1/graph/statistics
```

### Q5: 可以导入多个JSON文件吗？

**可以！** 系统支持增量导入：
1. 先导入第一个JSON文件
2. 再导入第二个JSON文件
3. 新节点会追加，重复ID的节点会更新

### Q6: 导入的数据会覆盖原有数据吗？

**不会完全覆盖！** 数据合并规则：
- **新节点**: 直接添加
- **重复ID节点**: 更新为新数据
- **新边**: 直接添加
- **重复ID边**: 更新为新数据

**清空数据**: 如需完全替换，请先清空数据：
1. 开发中可通过API: `DELETE /api/v1/import/clear`
2. 或重启后端服务（内存数据会清空）

---

## 六、API接口说明 🔌

### 6.1 导入JSON数据

**接口**: `POST /api/v1/import/json`

**请求体**:
```json
{
  "nodes": [
    {
      "id": "node-1",
      "type": "EntityType",
      "data": {}
    }
  ],
  "edges": [
    {
      "id": "edge-1",
      "source": "node-1",
      "target": "node-2",
      "type": "relation_type",
      "data": {}
    }
  ]
}
```

**响应**:
```json
{
  "success": true,
  "data": {
    "added_nodes": 69,
    "added_edges": 74,
    "total_nodes": 69,
    "total_edges": 74
  },
  "message": "数据导入成功"
}
```

### 6.2 清空所有数据

**接口**: `DELETE /api/v1/import/clear`

**响应**:
```json
{
  "success": true,
  "message": "所有数据已清空"
}
```

### 6.3 查询统计信息

**接口**: `GET /api/v1/graph/statistics`

**响应**:
```json
{
  "success": true,
  "data": {
    "total_nodes": 69,
    "total_edges": 74,
    "entity_counts": {
      "Epic": 3,
      "FeatureRequirement": 6,
      "SSTS": 4,
      ...
    }
  }
}
```

---

## 七、最佳实践 ✨

### 7.1 数据准备

1. **使用标准格式**: 参考`core-domain-data.json`的格式
2. **节点ID唯一**: 确保每个节点有唯一的ID
3. **边的source和target存在**: 确保边引用的节点ID存在
4. **添加元数据**: 包含version、name、description等信息
5. **数据验证**: 导入前用JSON验证工具检查格式

### 7.2 大数据集处理

**节点数 > 200**:
- 考虑分批导入
- 使用筛选功能只显示部分节点
- 使用追溯查询聚焦关注点

**追溯深度 > 5**:
- 减少追溯深度
- 使用分级查询
- 考虑性能优化

### 7.3 数据版本管理

**建议使用Git管理JSON数据文件**:
```bash
# 添加数据文件
git add data/my-domain-data.json

# 提交变更
git commit -m "feat: 添加核心领域模型数据 v1.0"

# 打标签
git tag v1.0.0
```

### 7.4 数据备份

**定期备份数据**:
```bash
# 导出当前数据（通过API或前端导出功能）
curl http://localhost:3001/api/v1/graph/data > backup-$(date +%Y%m%d).json

# 或复制数据文件
cp data/core-domain-data.json backups/core-domain-data-$(date +%Y%m%d).json
```

---

## 八、相关资源 📚

**文档**:
- [核心领域模型详细说明](./data/core-domain-README.md)
- [快速开始指南](./CORE_DOMAIN_QUICK_START.md)
- [实施总结](./IMPLEMENTATION_SUMMARY.md)
- [API文档](./docs/API.md)

**示例数据**:
- `data/core-domain-data.json` - 核心领域模型（69节点，74边）⭐
- `data/core-domain-schema.json` - Schema定义（22实体类型，20关系类型）
- `data/sample-data.json` - 智能驾驶样本数据
- `data/sample-triples.md` - 三元组样本数据

**工具**:
- [JSONLint](https://jsonlint.com/) - JSON格式验证
- [JSON Editor Online](https://jsoneditoronline.org/) - 在线JSON编辑器
- VS Code - 推荐使用的代码编辑器

---

## 九、技术支持 💬

**遇到问题？**
1. 查看浏览器控制台错误信息
2. 查看后端日志输出
3. 参考常见问题章节
4. 查阅API文档

**反馈渠道**:
- 技术支持: support@example.com
- 问题反馈: issues@example.com

---

**创建日期**: 2026-01-16  
**维护者**: 自动化研发平台团队  
**版本**: 1.0.0

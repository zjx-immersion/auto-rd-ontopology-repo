# 使用示例

本文档提供岚图智能驾驶知识图谱系统的实际使用场景和示例。

## 场景1：需求追溯 - 软件需求影响分析

### 业务场景
系统架构师修改了 `SYS_2001`（超声波雷达感知融合架构），需要快速了解这个变更会影响哪些下游需求和开发任务。

### 操作步骤

#### 方法1：通过前端界面

1. 启动系统，进入知识图谱页面
2. 在图谱中找到或搜索 `SYS_2001` 节点
3. 点击该节点，右侧弹出详情面板
4. 在"需求追溯"区域：
   - 选择查询类型：**影响分析**
   - 设置追溯深度：**3**
5. 点击"执行追溯查询"
6. 查看追溯结果：
   - **下游影响链**：显示所有受影响的软件需求、模块、模型版本
   - **变更影响分析**：
     - 受影响的实体数量
     - 通知的责任人列表
     - 风险等级评估
     - 预估工作量

#### 方法2：通过API调用

```bash
curl -X POST http://localhost:3001/api/v1/ontology/trace \
  -H "Content-Type: application/json" \
  -d '{
    "entity_id": "SYS_2001",
    "query_type": "impact_analysis",
    "depth": 3
  }'
```

**响应结果**：
```json
{
  "success": true,
  "data": {
    "query_entity": {
      "sys_id": "SYS_2001",
      "title": "超声波雷达感知融合架构"
    },
    "downstream_chain": [
      {
        "level": 1,
        "entity_type": "SWR",
        "entity_id": "SWR_5001",
        "title": "OD模型训练精度提升至96%",
        "owner": "张三"
      },
      {
        "level": 1,
        "entity_type": "SWR",
        "entity_id": "SWR_5002",
        "title": "行人检测模型优化",
        "owner": "李四"
      }
    ],
    "change_impact": {
      "affected_entities": {
        "swr": ["SWR_5001", "SWR_5002"],
        "modules": ["PF_OD", "PF_PD"],
        "packages": ["Daily_20260117"]
      },
      "notified_owners": ["张三", "李四"],
      "risk_level": "中",
      "estimated_effort": "28人时",
      "recommendation": "建议通知所有相关责任人，更新相关测试用例"
    }
  },
  "performance": {
    "duration_ms": 45
  }
}
```

### 业务价值
- ⏱️ 追溯时间：从 **2小时 → 2秒**
- 👥 自动通知：识别出需要通知的 **2位责任人**
- ⚠️ 风险评估：自动判断变更风险等级为 **中**
- 📊 工作量估算：预估影响范围需要 **28人时** 处理

---

## 场景2：数据导入 - 从Excel导入三元组

### 业务场景
测试团队维护了一份Excel表格，记录了所有测试用例与软件需求的关系，需要导入到知识图谱中。

### 准备Excel文件

创建 `test-cases.xlsx`，格式如下：

| 实体A (ID) | 关系 (谓语) | 实体B (ID) | 置信度 | 来源系统 | 更新时间 |
|------------|-------------|------------|--------|----------|----------|
| SWR_5001 | 验证于 | TC_OD_001 | 1.0 | TestRail | 2026-01-15 |
| SWR_5001 | 验证于 | TC_OD_002 | 1.0 | TestRail | 2026-01-15 |
| SWR_5002 | 验证于 | TC_PD_001 | 1.0 | TestRail | 2026-01-15 |
| TC_OD_001 | 发现 | ISSUE_301 | 1.0 | TestRail | 2026-01-18 |
| TC_OD_002 | 发现 | ISSUE_302 | 1.0 | TestRail | 2026-01-18 |

### 操作步骤

#### 方法1：通过前端界面

1. 点击顶部"导入数据"按钮
2. 选择"Excel文件"标签
3. 点击"选择Excel文件"按钮，选择准备好的文件
4. 系统自动解析Excel内容
5. 显示解析结果：如 "文件已加载 (5 行数据)"
6. 点击"导入"按钮
7. 成功提示：`导入成功: 7个节点, 5条边`

#### 方法2：使用xlsx库处理后通过API导入

```javascript
const XLSX = require('xlsx');
const axios = require('axios');

// 读取Excel文件
const workbook = XLSX.readFile('test-cases.xlsx');
const worksheet = workbook.Sheets[workbook.SheetNames[0]];
const data = XLSX.utils.sheet_to_json(worksheet, { header: 1 });

// 导入数据
axios.post('http://localhost:3001/api/v1/import/excel', {
  data: data,
  type: 'triples'
}).then(response => {
  console.log('导入成功:', response.data);
});
```

### 验证导入结果

在图谱中搜索 `TC_OD_001`，应该能看到：
- 新增的测试用例节点
- 与 `SWR_5001` 的"验证于"关系
- 与 `ISSUE_301` 的"发现"关系

---

## 场景3：完整追溯 - 从项目到测试问题

### 业务场景
测试发现了一个P1级别的问题 `ISSUE_301`，项目经理需要了解这个问题影响哪个项目，来源于哪个需求。

### 操作步骤

1. 在图谱中点击 `ISSUE_301` 节点
2. 查看详情：
   - 问题描述："雨天场景OD模型漏检率超标2%"
   - 严重级别：P1
   - 状态：修复中
3. 执行追溯查询：
   - 查询类型：**完整追溯**
   - 深度：**5**
4. 查看完整追溯链：

**向上追溯链**（问题根源）：
```
Level 1: TC_OD_001 (测试用例: 雨天泊车OD测试)
         ↑ 验证于
Level 2: SWR_5001 (软件需求: OD模型训练精度提升至96%)
         ↑ 追溯至
Level 3: SYS_2001 (系统架构: 超声波雷达感知融合架构)
         ↑ 拆解为
Level 4: SSTS_1001 (系统需求: 自动泊车功能系统需求)
         ↑ 管理
Level 5: PROJ_001 (车型项目: GOP平台智能驾驶项目)
```

### 分析结果

通过追溯，项目经理得知：
- 🎯 **影响项目**：GOP平台智能驾驶项目 (PROJ_001)
- 📋 **来源需求**：自动泊车功能系统需求 (SSTS_1001)
- 👤 **责任人**：张三（SWR_5001的owner）
- ⏰ **里程碑影响**：可能影响PVC里程碑交付
- 🔧 **建议措施**：
  1. 通知项目经理赵六
  2. 协调张三加急修复
  3. 重新评估PVC里程碑风险

---

## 场景4：批量查询 - 评估多个需求的状态

### 业务场景
项目例会前，项目经理需要快速了解所有软件需求的完成情况和测试覆盖。

### API调用

```bash
curl -X POST http://localhost:3001/api/v1/ontology/trace/batch \
  -H "Content-Type: application/json" \
  -d '{
    "entity_ids": ["SWR_5001", "SWR_5002", "SWR_5003"],
    "query_type": "full_trace",
    "depth": 2
  }'
```

### 响应处理

```javascript
const results = response.data.data;

// 生成汇总报告
const summary = results.map(r => ({
  需求ID: r.data.query_entity.id,
  标题: r.data.query_entity.title,
  负责人: r.data.query_entity.owner,
  状态: r.data.query_entity.status,
  测试覆盖: `${r.data.test_coverage.passed}/${r.data.test_coverage.total_test_cases}`,
  未解决问题: r.data.test_coverage.issues.length
}));

console.table(summary);
```

**输出示例**：
```
┌─────────┬──────────┬────────────────────────┬────────┬─────────┬──────────┬────────────┐
│ (index) │  需求ID   │         标题           │ 负责人 │  状态   │ 测试覆盖 │ 未解决问题 │
├─────────┼──────────┼────────────────────────┼────────┼─────────┼──────────┼────────────┤
│    0    │SWR_5001  │OD模型训练精度提升至96%  │  张三  │ 开发中  │   0/1    │     1      │
│    1    │SWR_5002  │行人检测模型优化         │  李四  │ 开发中  │   1/1    │     0      │
│    2    │SWR_5003  │车道线检测模块开发       │  王五  │ 待测试  │   1/1    │     0      │
└─────────┴──────────┴────────────────────────┴────────┴─────────┴──────────┴────────────┘
```

---

## 场景5：搜索与过滤

### 搜索特定关键词

#### 前端搜索
在左侧边栏的搜索框输入关键词，如 "泊车"

#### API搜索
```bash
curl "http://localhost:3001/api/v1/graph/search?keyword=泊车"
```

### 按类型过滤节点

```bash
# 获取所有软件需求
curl "http://localhost:3001/api/v1/graph/nodes?type=SWR"

# 获取所有模型版本
curl "http://localhost:3001/api/v1/graph/nodes?type=ModelVersion"
```

### 按关系类型过滤边

```bash
# 获取所有"拆解为"关系
curl "http://localhost:3001/api/v1/graph/edges?type=decomposes_to"

# 获取某个节点的所有出边
curl "http://localhost:3001/api/v1/graph/edges?source=SYS_2001"
```

---

## 场景6：实时监控 - 模型发版风险预警

### 业务场景
监控系统定期检查所有训练中的模型，当模型即将完成但距离发版时间不足7天时，自动发送预警。

### 实现逻辑

```javascript
const axios = require('axios');

async function checkModelReleaseRisk() {
  // 获取所有模型版本节点
  const models = await axios.get(
    'http://localhost:3001/api/v1/graph/nodes?type=ModelVersion'
  );

  const today = new Date();
  const warnings = [];

  for (const model of models.data.data) {
    if (model.data.status === '量化中') {
      const releaseDate = new Date(model.data.release_date);
      const daysRemaining = Math.ceil((releaseDate - today) / (1000 * 60 * 60 * 24));

      if (daysRemaining < 7) {
        // 执行追溯查询，获取相关责任人
        const trace = await axios.post(
          'http://localhost:3001/api/v1/ontology/trace',
          {
            entity_id: model.id,
            query_type: 'impact_analysis'
          }
        );

        warnings.push({
          model_id: model.id,
          model_type: model.data.model_type,
          days_remaining: daysRemaining,
          risk_level: 'P1',
          owners: trace.data.data.change_impact.notified_owners,
          recommendation: '建议延迟发版或加速量化流程'
        });
      }
    }
  }

  // 发送预警通知（集成飞书/钉钉等）
  if (warnings.length > 0) {
    console.log('⚠️ 发现', warnings.length, '个模型发版风险');
    console.table(warnings);
    // sendNotification(warnings);
  }
}

// 每小时检查一次
setInterval(checkModelReleaseRisk, 3600000);
```

---

## 性能基准

基于样本数据（22个节点，22条边）的性能测试结果：

| 操作 | 响应时间 | 说明 |
|------|---------|------|
| 加载图谱数据 | ~50ms | 包含所有节点和边 |
| 单节点查询 | ~5ms | 根据ID查询 |
| 搜索节点 | ~20ms | 关键词搜索 |
| 完整追溯（深度3） | ~45ms | 上下游链路 |
| 影响分析（深度5） | ~80ms | 包含影响评估 |
| 导入100条三元组 | ~150ms | Markdown格式 |

**预估规模化性能**（1000个节点，3000条边）：
- 图谱加载：< 200ms
- 追溯查询：< 500ms
- 搜索：< 100ms

---

## 最佳实践

### 1. 数据命名规范
- 使用一致的ID前缀（PROJ_、SSTS_、SWR_等）
- 标题简洁明确，长度控制在100字符内
- 关键属性（如owner、status）必填

### 2. 追溯查询优化
- 根据需求选择合适的查询类型
- 深度设置在3-4层最佳，避免过深导致性能下降
- 批量查询时控制在10个实体以内

### 3. 数据导入建议
- 大批量导入建议使用JSON格式
- Excel导入前先验证数据格式
- 定期备份图谱数据

### 4. 可视化技巧
- 节点过多时使用搜索功能快速定位
- 点击节点后查看邻居关系
- 使用缩放和拖拽优化查看体验

---

## 扩展集成

### 与飞书集成
```javascript
// 发送追溯结果到飞书群
async function sendToFeishu(traceResult) {
  await axios.post('https://open.feishu.cn/open-apis/bot/v2/hook/xxx', {
    msg_type: 'interactive',
    card: {
      header: { title: { content: '需求追溯结果' } },
      elements: [
        { tag: 'div', text: `实体: ${traceResult.query_entity.id}` },
        { tag: 'div', text: `影响范围: ${traceResult.change_impact.impact_score}` }
      ]
    }
  });
}
```

### 与CI/CD集成
在GitLab CI中自动更新知识图谱：

```yaml
# .gitlab-ci.yml
update-knowledge-graph:
  stage: deploy
  script:
    - |
      curl -X POST http://ontology-api:3001/api/v1/graph/nodes \
        -H "Content-Type: application/json" \
        -d "{
          \"id\": \"COMMIT_${CI_COMMIT_SHORT_SHA}\",
          \"type\": \"CodeCommit\",
          \"data\": {
            \"commit_hash\": \"${CI_COMMIT_SHA}\",
            \"author\": \"${CI_COMMIT_AUTHOR}\"
          }
        }"
```

---

需要更多帮助？查看 [API文档](./API.md) 或 [快速启动指南](./QUICK_START.md)。
